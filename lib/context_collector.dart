/// Context Collector
/// A Flutter package for collecting and displaying context from scanned files.
/// its usable for many usecases, the most common is to collect context from a AI model.
/// It saved copy paste time and provides a clean interface for managing file content.
/// it gives paths to files, which is useful for AIs to understand the context of the files, and how they relate to each other.
/// also there are many other usecases, but this is the most common one.
library;

export 'src/context_collector.dart';
